import React from 'react';
import '../../../styles/gold-text.css';

export function NetworkTitle() {
  return (
    <h3 className="gold-gradient-text gold-shimmer text-sm font-medium text-center font-montserrat">
      Cards Per Network
    </h3>
  );
}